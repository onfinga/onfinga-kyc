services:
  - type: web
    name: onfinga-kyc-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:10000 main:app
    envVars:
      - key: FLASK_ENV
        value: production
